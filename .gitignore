# node — lives in setup/ so it stays out of the repo root
node_modules/

# python
__pycache__/
*.py[cod]
.venv/
venv/

# generated tool state, all under one roof (see pyproject.toml)
.cache/

# analysis outputs — regenerate with the scripts, don't commit
plots/
rnd/analysis/plots/

# recorded sessions live outside the repo (see setup/README.md); if any land
# here by accident, don't commit megabytes of CSV
bar-data/
*.wplay

# os
.DS_Store
Thumbs.db
